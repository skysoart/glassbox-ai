# Replay System

Every run trace captures sufficient contextual and tool-result data to identically replay an execution sequence historically without making live API calls to tools that might return changed data.
